interface:
  display_name: "Monitor Remote Jobs"
  short_description: "Run and await long jobs without model polling"
  default_prompt: "Use $monitor-remote-jobs to start this command as a tracked job and continue when it finishes."
