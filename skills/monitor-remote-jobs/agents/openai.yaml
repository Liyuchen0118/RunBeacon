interface:
  display_name: "RunBeacon Remote Jobs"
  short_description: "Default tracked routing for SSH and remote jobs"
  default_prompt: "Use $monitor-remote-jobs to run this command on the remote server and continue when it finishes."

policy:
  allow_implicit_invocation: true
